# ttllm-nano-v3

Expanded Project Gutenberg public-domain corpus (6 sources). Character LM, 600 steps, dense checkpoints.

Data sources in `data/processed/sources.json`. Canary-injected ttlink index.

Reproduce: train.py --steps 600 after prepare_data on expanded set.
