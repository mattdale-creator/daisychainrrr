# Wall 2 — R2

## Cannot agent-close
Account R2 enable when API returns **10042** until dashboard accepts R2.

## Agent bone
| Artefact | Path |
|----------|------|
| Handbook gate | `docs/handbook/gates/02-r2.md` |
| Bucket design | `ops/r2/BUCKET_DESIGN.md` |
| Wrangler stub | `ops/r2/wrangler.r2.example.toml` |
| Upload runbook | `ops/r2/UPLOAD_RUNBOOK.md` |
| Env example | `ops/secrets.local.env.example` keys for R2 |

## Design (ready when enabled)
- Bucket: `ttllm-public-releases` (public-read free-core only)
- Never put tenant data in public bucket
- Every object SHA-256 in a manifest leaf
- Lifecycle: versioning on; no silent delete of published free-core objects

## Human close
Dashboard → R2 → enable → create bucket → API token R2 write → run upload runbook → update ASSET_INVENTORY.
